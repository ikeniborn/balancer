# Balancer PRD - Руководство по документам

## 📚 Доступные документы

### 1️⃣ balancer-prd-complete.md (127 KB) - ПОЛНЫЙ ДОКУМЕНТ
**Рекомендуется для начала!**

Объединенный документ, включающий:
- Основной ПРД со всеми конфигурациями
- Расширение с архитектурой прямого подключения фронтендов
- Все Mermaid диаграммы
- Полные скрипты

[📥 Скачать полный документ](computer:///mnt/user-data/outputs/balancer-prd-complete.md)

---

### 2️⃣ balancer-prd.md (83 KB) - Основной ПРД
Базовый документ с основной архитектурой

[📥 Скачать основной ПРД](computer:///mnt/user-data/outputs/balancer-prd.md)

---

### 3️⃣ balancer-prd-extension.md (45 KB) - Дополнение
Расширение про прямое подключение фронтендов

[📥 Скачать дополнение](computer:///mnt/user-data/outputs/balancer-prd-extension.md)

---

## 🎯 Ключевые архитектурные решения

### Типы подключения сервисов:

```
┌─────────────────────────────────────────────────────────┐
│  1. Backend API (Proxy)                                 │
│     FastAPI, Django, Node.js → Nginx Proxy → Internet   │
│     Использование: API сервисы                          │
├─────────────────────────────────────────────────────────┤
│  2. Frontend Static (Direct Mount) ⭐ НОВОЕ             │
│     Static files → Nginx (Volume) → Internet            │
│     Использование: React/Vue/Angular билды              │
│     Преимущества: -50% RAM, быстрее, проще деплой       │
├─────────────────────────────────────────────────────────┤
│  3. Frontend Container (Proxy)                          │
│     Next.js/Nuxt → Nginx Proxy → Internet               │
│     Использование: SSR приложения                       │
├─────────────────────────────────────────────────────────┤
│  4. Database (TCP Stream)                               │
│     PostgreSQL/CouchDB → Nginx Stream → Internet        │
│     Использование: Базы данных                          │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 Когда использовать Direct Mount?

### ✅ Используйте Direct Static Mount для:

- **React приложения** (после npm run build)
- **Vue.js приложения** (после npm run build)
- **Angular приложения** (после ng build)
- **Статические сайты** (HTML/CSS/JS)
- **Landing pages**
- **Документация** (VuePress, Docusaurus)

**Преимущества:**
```
Proxy Container:  Client → Nginx-Proxy → Frontend-Nginx → Files
                         (80 MB RAM)    (80 MB RAM)
                  
Direct Mount:     Client → Nginx-Proxy → Files
                         (80 MB RAM)
```

### ⚠️ Используйте Proxy Container для:

- **Next.js с SSR** (Server Side Rendering)
- **Nuxt.js с SSR**
- **Сложные Nginx конфигурации**
- **Когда нужна изоляция** в отдельном контейнере

---

## 🚀 Быстрый старт

### 1. Инициализация системы

```bash
cd /opt/balancer
sudo bash scripts/init-balancer.sh
```

Скрипт спросит:
- Email для Let's Encrypt
- Telegram Bot Token
- Telegram Chat ID
- Timezone

### 2. Добавление фронтенда (Static)

```bash
sudo bash scripts/add-service.sh

# Выбрать:
# - Категория: 2 (Frontend Static)
# - Имя: myapp
# - Поддомен: app
# - Домен: example.ru

# Результат:
# ✓ https://app.example.ru готов
# ✓ Директория: /opt/balancer/static/myapp/
```

### 3. Деплой React приложения

```bash
# На локальной машине
cd my-react-app
npm run build

# Деплой на сервер
rsync -av --delete dist/ user@server:/opt/balancer/static/myapp/

# Или через скрипт
bash /opt/balancer/scripts/deploy-frontend.sh myapp ./dist
```

### 4. Добавление Backend API

```bash
sudo bash scripts/add-service.sh

# Выбрать:
# - Категория: 1 (Backend API)
# - Тип: 1 (FastAPI)
# - Контейнер: fastapi-container
# - Порт: 8000
```

---

## 📁 Структура директорий

```
/opt/balancer/
├── nginx/
│   ├── conf.d/
│   │   ├── proxy/          ← Backend API конфиги
│   │   ├── static/         ← Static Frontend конфиги ⭐
│   │   ├── external/       ← Proxy Frontend конфиги
│   │   └── streams.conf    ← TCP Database конфиги
│   └── snippets/
│
├── static/                 ← Статический контент ⭐
│   ├── myapp/             ← React/Vue/Angular билды
│   ├── landing/
│   └── docs/
│
├── scripts/
│   ├── add-service.sh     ← Добавление сервиса (обновлен)
│   ├── deploy-frontend.sh ← Деплой статики ⭐
│   └── rollback-frontend.sh ← Откат версии ⭐
│
└── deployments/            ← История деплоев ⭐
    └── myapp/
        ├── 2025-11-06_10-30-15/
        └── current → ...
```

---

## 🔄 Workflow разработки

### Frontend Static (React/Vue/Angular)

```mermaid
graph LR
    A[Разработка] --> B[npm run build]
    B --> C[rsync → /static/app/]
    C --> D[nginx -s reload]
    D --> E[✓ Live]
    
    style A fill:#2196F3
    style E fill:#4CAF50
```

### Откат версии

```bash
# Мгновенный откат на предыдущую версию
bash scripts/rollback-frontend.sh myapp previous

# Откат на конкретную версию
bash scripts/rollback-frontend.sh myapp 2025-11-06_10-30-15
```

---

## 📊 Сравнение подходов

| Критерий | Static Mount | Proxy Container |
|----------|-------------|-----------------|
| RAM | 80 MB | 160 MB |
| Latency | 2-5 ms | 5-10 ms |
| Деплой | Copy + reload | Build + restart |
| Rollback | Мгновенно | Rebuild |
| Сложность | ⭐ Низкая | ⭐⭐ Средняя |
| **Рекомендация** | **SPA билды** | **SSR, кастом** |

---

## 🎨 Mermaid диаграммы в документах

Документ содержит **7 интерактивных диаграм**:

1. ✅ Общая архитектура системы
2. ✅ Типы подключения сервисов
3. ✅ Поток обработки HTTPS запроса
4. ✅ Процесс добавления сервиса
5. ✅ Обновление SSL сертификатов
6. ✅ Docker сети и взаимодействие
7. ✅ Workflow развертывания

---

## 🛠 Что включено

### Готовые скрипты:

- ✅ `init-balancer.sh` - Первоначальная настройка
- ✅ `add-service.sh` - Добавление сервиса (4 типа)
- ✅ `remove-service.sh` - Удаление сервиса
- ✅ `deploy-frontend.sh` - Деплой статики
- ✅ `rollback-frontend.sh` - Откат версии
- ✅ `health-check.sh` - Мониторинг
- ✅ `telegram-alert.sh` - Уведомления

### Готовые конфигурации:

- ✅ Docker Compose с volume mount
- ✅ Nginx главный конфиг
- ✅ 5+ шаблонов для разных типов сервисов
- ✅ SSL параметры
- ✅ Security headers
- ✅ UFW + Fail2Ban

### Примеры для:

- ✅ FastAPI Backend
- ✅ React/Vue/Angular Static
- ✅ Next.js/Nuxt SSR
- ✅ PostgreSQL
- ✅ CouchDB

---

## 📞 Поддержка

**Документация:**
- Основной ПРД: 83 KB
- Расширение: 45 KB
- Полный документ: 127 KB

**Следующие шаги:**
1. Прочитайте полный документ
2. Запустите `init-balancer.sh`
3. Добавьте первый сервис
4. Настройте мониторинг

---

## 💡 Ключевые инновации

### 🆕 Прямое подключение фронтендов

Вместо:
```
Nginx → Nginx Container → Static Files
```

Используем:
```
Nginx → Static Files (Volume)
```

**Результат:**
- ⚡ Быстрее на 50%
- 💾 Меньше RAM на 50%
- 🚀 Проще деплой
- ⏪ Мгновенный rollback

---

**Готово к использованию!** 🎉

Начните с [полного документа](computer:///mnt/user-data/outputs/balancer-prd-complete.md)
